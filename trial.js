// const arr = ["a", "A", "b", "B", 1, 2, 3, "1", "2"];
// console.log(arr.sort()); // this is beacuse of the ascii value alloted to each character but in case of number and text format for the number=> more priority is given to the number and then to its string

// Calculate week number
/* function getWeekNum(date) {
  let start = new Date(date.getFullYear(), 0, 1);
  console.log(start);
  const diff = date - start;
  console.log(diff);

  const oneWeek = 1000 * 60 * 60 * 24 * 7;
  return Math.ceil((diff + 1) / oneWeek);
}

const today = new Date("2025-01-10");
console.log("Simple week number: ", getWeekNum(today));

// Diff in days
let date1 = new Date("2025-07-01");
console.log(date1);

let date2 = new Date("2025-06-01");
console.log(date2);

let diffTime = Math.abs(date2 - date1);
console.log(diffTime);
let diffDay = Math.ceil(diffTime / (1000 * 60 * 60 * 24));

console.log("Difference between the days are: " + diffDay);

const pi=3.14;
pi=8.9;
console.log(pi); */

for(let i=)